spring:
  application:
    name: taskmaster-server
  profiles:
    active: dev
  config:
      import: optional:configserver:http://localhost:8071

#todo настроить actuator
management:
  endpoints:
    web:
      exposure:
        include: "*"
